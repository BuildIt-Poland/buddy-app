interface Dictionary {
  OPTIONS: {
    ADD_BUDDY: string;
    ADD_TALENT: string;
  };
}

const DICTIONARY: Dictionary = {
  OPTIONS: {
    ADD_BUDDY: 'Add new Buddy',
    ADD_TALENT: 'Add new Talent',
  },
};

export default DICTIONARY;
