import * as dataPL from "./seed-data.pl";
import * as dataID from "./seed-data.id";
import * as dataUS from "./seed-data.us";
import * as dataUK_IE from "./seed-data.uk-ie";

export const newbiesData = [dataPL, dataUS, dataID, dataUK_IE];

export const buddyData = {
  email: "TASKS_TEMPLATES",
  name: "",
  password: ""
};
