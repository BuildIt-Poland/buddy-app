overwrite: true
schema: "./src/schema.graphql"
documents: null
generates:
  src/generated/types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  src/generated/schema.json:
    plugins:
      - "introspection"
