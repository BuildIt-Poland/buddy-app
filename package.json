{
  "name": "buddy-app",
  "scripts": {
    "bootstrap": "lerna bootstrap --use-workspaces"
  },
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/@bahmutov/add-typescript-to-cypress",
      "**/@bahmutov/add-typescript-to-cypress/**"
    ]
  },
  "devDependencies": {
    "lerna": "^3.8.0"
  }
}
