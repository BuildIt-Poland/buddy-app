language: node_js
node_js:
  - "stable"
cache:
  directories:
    - node_modules
before_install:
  - npm install -g codecov
script:
  - npm run test:ci
  - npm run lint
  - npm run build
after_success:
  - codecov --token="$CODECOV_TOKEN"
notifications:
  slack: $SLACK_TOKEN
